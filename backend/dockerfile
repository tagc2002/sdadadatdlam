FROM ghcr.io/astral-sh/uv:debian-slim

WORKDIR /usr/app/src

COPY pyproject.toml uv.lock ./

RUN uv sync --no-cache

COPY . .

EXPOSE 80/tcp
# escape=\
CMD ["uv", "run", "fastapi", "run", "main.py", "--port", "80", "--host", "0.0.0.0", "--forwarded-allow-ips=\"*\"", "--proxy-headers"]